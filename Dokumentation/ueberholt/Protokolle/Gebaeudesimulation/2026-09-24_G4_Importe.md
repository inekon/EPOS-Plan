# Protokoll: Stufe G4 — Importe (gbXML G4c, IFC G4a), ab 24.09.2026

**Auftrag** (vom Anwender am 24.09.2026 beauftragt, Entscheid E38): Stufe G4 der Gebäudesimulation —
zuerst **G4c** gbXML-Import (Lesemodell und Einheiten, Aggregation auf das Zonenmodell, Zuordnungsdialog
mit Herkunft je Feld, Persistenz der Zuordnung Schemaschritt S-F, selbst erzeugte Beispieldateien), danach
**G4a** IFC-Import (xBIM-Paket und Lizenzhinweisseite U10, Leser hinter `IGebaeudeLeser`, Azimut,
Zuordnung, Vorgabetabelle nach Baualtersklasse, Dialog und Hülle, Importprobe, Größenlimit). **G4b** erst
nach G3 und nachdem G4a im Feld war. Maßgeblich:
[Datenaustauschkonzept](../../../aktuell/Konzept_Datenaustausch_gbXML_IFC_EPOS-Plan.md) 2, 3, 7–10,
[Umsetzungskonzept](../../../aktuell/Umsetzungskonzept_Gebaeudesimulation_VDI6007_EPOS-Plan.md) 3,
[Softwarearchitektur](../../../aktuell/Softwarearchitektur_Gebaeudesimulation_EPOS-Plan.md) 1.2, 1.5, 2.7, 3.4,
[ADR-004](../../../aktuell/ADR-004_gbXML_LINQ_to_XML.md),
[Konzept Gebäudesimulation](../../../aktuell/Konzept_Gebaeudesimulation_VDI6007_EPOS-Plan.md) N1.43 (E38).

**Parallelität.** Die Sitzung „Gebäudesimulation G3 Bauteilkatalog" legt die Schemaschritte S-A bis S-C
(`Tab_Baustoff`, `Tab_Bauteilaufbau`/`-schicht`, `Tab_Zone`, `Tab_Bauteil`) und die Funktion „U-Wert aus
Schichten" (`Simulation/Gebaeude/Bauteilreduktion.cs`) an; S-F (`Tab_Importquelle`,
`Tab_Importzuordnung`) folgt erst danach, die Kaskadenrettung hängt an deren Tabellenliste. Den Einstieg in
den Gebäudedialog bindet G4 erst an, wenn die Welle 3 der Anlagenkopplung (AK1) auf origin liegt.

## 1 G4c Welle 1 — Kern-Leser und gemeinsames Importgerüst (24.09.2026)

Ohne Schemaschritt, ohne Oberfläche; es wird nichts in die Datenbank geschrieben.

| Teil | Stand |
|---|---|
| Gerüst `EPOS.Kern/Allgemein/Import/Gebaeude/` (formatfrei, von G4a mitbenutzt) | `Importherkunft` (sechs Werte, dazu die Persistenzwerte `GBXML`/`IFC`/`KATALOG`/`MANUELL`/`VORGABE`), `GebaeudeQuelle` (Dateiname ohne Pfad, SHA-256, Größe, Versionswert, Zeitpunkt, Zonenregel), `GebaeudeZielfelder` (33 Zielfelder in 9 Gruppen), `GebaeudeFeldzeile`/`GebaeudeBeleg` (Wert, Herkunft, Beleg als Schlüssel und invariante Werte, Vorgabewert, Haken, Markierung), `GebaeudeImportSatz` (samt Quellzuordnungen für S-F), `GebaeudeAbbild` (normiertes Zwischenmodell, Einheit im Namen), `IGebaeudeLeser` (A2), `GebaeudeImportProfil`, `GebaeudeImportAblauf` (Lesen mit Größenablehnung vor dem Parsen, SHA-256, `Zuordnen`, `Pruefen`), `GebaeudeAggregation` (die gemeinsame E2-Zuordnung), `GebaeudeVorgaben` (U12), `Quellkennung` (56 Zeichen und 8 Hex-Zeichen SHA-256), `Raumnamenregel`, `GebaeudeZuordnungsModell` (alle Anzeigetexte), `SchichtwerteNaht` (ruft `Bauteilreduktion` aus G3) |
| gbXML-Leser `EPOS.Kern/Allgemein/Import/Gbxml/` | `GbxmlLeser` (LINQ to XML über den Strom, DTD verboten, kein Auflöser, keine Schemaprüfung, mit und ohne Namensraum, zweistufig, Geometrie aus `RectangularGeometry`, sonst aus `PolyLoop` nach Newell), `GbxmlEinheiten` (global und lokal, unbekannt → leer mit Warnung), `GbxmlAbbild`, `GbxmlImportProfil` (25 MB Windows / 10 MB iOS, D15) |
| Vorgaben je Baualtersklasse (U12) | Median der Katalogsätze je Klasse aus `Tab_Gebaeude_STAMM` (U-Werte, g-Wert, drei ψ); die Klassen L, O, P, R, T und U ohne Katalogsatz bleiben leer; der Test rechnet den Median aus der Testdatenbank nach |
| E38 | U13 (eines je Lauf über `Zuordnen(gebaeudeIndex, …)`), U14 (A_Wand = Wandfläche − Fenster − Außentüren, negativ → 0, Zeile rot, `IMP_GBXML_PROT_NETTOFLAECHE_NEGATIV`), U15 (ψ als Vorgabe, Anschlusslängen leer) |
| Meldungen und Texte | 122 Schlüssel in beiden `.resx`: die 18 aus Datenaustauschkonzept 3.8, 7 weitere `IMP_GBXML_PROT_*`, 24 `IMP_GEB_PROT_*`, 73 `GIMP_*`; `ResourceDesigner` neu erzeugt |
| Importproben | 11 selbst erzeugte Dateien unter `Referenzlaeufe/Importproben/`: das Probenhaus in SI, in Fuß/Fahrenheit/BTU und als UTF-16LE, dazu acht Kleinstdateien unter 5 KB je Fehlerbild; neu `LIESMICH_Importproben.md` mit Zweck, Herkunft und Lizenzstand aller 39 Proben des Ordners |
| Tests | 98 Fälle: Proben 4–9, 21 und 24 des Datenaustauschkonzepts, U13–U15, E2-Regeln (flächengewichtetes U, 30-%-Regel, Sektorgrenzen, Keller/Unbeheizt/Sonstige), Größenablehnung, Lesefehler samt externer Entität, Abbruch, U-Wert aus Schichten über `Bauteilreduktion`, Vorgaben gegen die Testdatenbank |

**Arbeitsentscheide der Welle** (Modell vor Papier, jeweils am Rechenweg geprüft):

1. **Innere Gewinne bleiben leer.** gbXML liefert Auslegungsleistungen für Licht und Geräte, die mit
   Zeitplänen gemeint sind; das Modell rechnet mit einem zeitlich konstanten Gewinn. Die Summe steht nur als
   Vorschlag im Beleg.
2. **`DesignCoolT` wird nicht zugeordnet** — `Maximaleraumtemperatur` ist nach E32 die Überhitzungsgrenze,
   kein Sollwert. `DesignHeatT` geht auf den Tagsollwert, nur wenn alle beheizten Räume denselben Wert tragen.
3. **Wände und Decken gegen Erdreich zählen zur Grundfläche mit `ERDREICH`** — im Einzonenmodell rechnet nur
   die Grundfläche gegen das Erdreich, Wand, Dach und Sonstige rechnen gegen Außenluft.
4. **Boden oder Decke gegen einen unbeheizten Raum:** zuerst entscheidet `AdjacentSpaceId/@surfaceType`, dann
   die Neigung, dann die Flächenart; Boden → Grundfläche `KELLER`, Decke → Dach, Wand → Sonstige (je mit Hinweis).
5. **Die Markierung „rot" sperrt nur bei negativer Nettofläche.** Gelb sind fehlende Geometrie, unbekannter
   Nachbar, Fenster ohne Himmelsrichtung und gemischte Randbedingung; „Zeile rot" im Datenaustauschkonzept 3.8
   meint die Bauteilzeilen von G6c.
6. **Kleinere Regeln:** `IMP_GBXML_PROT_OHNE_AUFBAU` einmal je Datei mit Zahl und Beispielen;
   `FreestandingColumn` übergangen, `EmbeddedColumn` zu den Sonstigen; Wirtsflächen unter 10° Neigung ohne
   Himmelsrichtung (Oberlichter im Flachdach gleichmäßig verteilt); Öffnungen der Art `Air` ohne Abzug;
   Luftwechsel volumengewichtet und nur, wenn jeder beheizte Raum einen trägt (D12: nur Infiltration).
7. **Zum Hinnehmen, benannt:** Nach D12 rechnet das Modell mit gelesenem Luftwechsel plus 0,4 (Vorgabe
   Nutzerlüftung). gbXML legt nicht fest, ob `SolarHeatGainCoeff` für die Verglasung oder das ganze Fenster
   gilt; EPOS setzt ihn als g-Wert der Verglasung an und mindert zusätzlich um den Rahmenanteil.

**Bewusst leer:** innere Gewinne, Bauweise (die Regel aus Schichten folgt mit Welle 2), Anschlusslängen (U15),
Nutzerlüftung (D12), Tagsollwert ohne einheitliche Angabe. Rahmenanteil, Verschattung, Masseanteil und
Innenflächenfaktor sind keine Zielfelder und bleiben bei der Vorgabe des Modells.

**Nachweis.** Build Kern-Filter und Windows-Schale 0 Fehler; volles Test-Gate grün (EPOS.Kern.Tests 6 226,
EPOS.UI.Tests 6 020, SpeicherEngine 386, SpeicherPlanung 27 und 1 fremd übersprungen, KiKern 549);
`SqlDialektPruefer` 1 830 SQL-Texte, 0 Fundstellen; `Auslieferungsvorlage.Tests` 30/30; `ResourceDesigner`
wiederholbar; Referenzlauf aller dreizehn Projekte gegen `2026-09-24_R14_Kaelteerzeuger` **13/13 PASS,
byte-gleich** (außer `protokoll.txt`) — nach dem Zusammenführen mit origin (Schemastand 130).

**Offen für die nächsten Wellen.** Welle 2: `GebaeudeImportDialog` (`EPOS.UI/Dialoge/Import/`) mit Hülle
`GebaeudeImportHuelle` (`EPOS.UI.Daten/Bedarf/`), Raumliste mit Haken, Baualtersklasse als erstes Feld,
Bauweise aus Schichten, Abbildung auf die Felder des Gebäudeeditors. Welle 3: Schemaschritt S-F nach dem S-C
der G3-Sitzung, `GebaeudeImportCtrl`, Kaskadenrettung, Registerpflege, Reduzierskript,
Auslieferungsvorlage. Welle 4: Einstieg im Gebäudedialog nach AK1 W3, Wiki-Quelle, Logbuch.

**CI zu Welle 1.** Der eigene Kern-Lauf wurde durch nachfolgende Pushes anderer Sitzungen abgebrochen; der
erste vollständige Lauf, der Welle 1 enthält, ist `kern.yml` 36067855651 — grün.

## 2 G4c Welle 2 — Zuordnungsdialog und Hülle (25.09.2026)

Noch nicht im Gebäudedialog angebunden, ohne Schemaschritt, ohne Datenbankzugriff.

| Teil | Stand |
|---|---|
| Kern | `GebaeudeImportAblauf.Raeume` (Raumliste mit Grund: Attribut, Namensregel, Annahme, Anwender), `Zuordnen` mit Übersteuerung „beheizt" je Raum, Bauart aus den raumseitigen Schichten bis 10 cm (`SchichtwerteNaht.WirksameKapazitaetAusSchichten` über `Bauteilreduktion`, flächengewichtet über die opaken Hüllbauteile, Warnung außerhalb 5…200 Wh/(m²K)), Hinweis im Beleg des g-Werts (Ganzfenster und Rahmenanteil), `ManuellSetzen`/`HakenSetzen`, `Pruefen` prüft auch den Namen, `GrenzeFuerPlattform` im Profil |
| Dialog | `EPOS.UI/Dialoge/Import/GebaeudeImportDialog.razor` mit `GebaeudeImportDaten.cs` (DTOs ohne Kern-Typen, Ergebnis-Record, Textbündel): Baualtersklasse als erstes Feld, Gebäudewahl bei mehreren Gebäuden (U13), Name des neuen Gebäudes, Raumliste mit Haken, Zeilen je Zielfeld mit Wert, Beleg, Vorgabe, Herkunft und Haken (Handänderung → Herkunft „manuell"), Meldungen, Fortschritt mit Abbruch, Größenablehnung vor dem Lesen; was je Format verschieden ist, kommt als Daten herein |
| Hülle | `EPOS.UI.Daten/Bedarf/GebaeudeImportHuelle.cs` (Instanz, der Zustand lebt im Ablauf; Dateiwahl über `Dienste.Datei.DateiOeffnenAsync`, Lesen über `Kulturweitergabe.Starten`, Grenze je Plattform) und `NachKatalogdaten` — die Abbildung der übernommenen Zeilen auf die Felder des Gebäudeeditors (Weg „als Katalogsatz ablegen"); Zeilen ohne Haken lassen die Grundlage stehen |
| Texte, Stil | 59 neue Schlüssel in beiden `.resx`; Stilregeln in `epos-ui.css`; Parameter `SpeichernLeiste.OkSperrgrund` |
| Tests | 17 Kern-Fälle (`GebaeudeImportWelle2Tests`), 19 bunit-Fälle des Dialogs (Rückweg, Abbruch ruft keine Übernahme, Haken, Handänderung, Klassenwechsel behält Handänderungen, Größenablehnung, ohne Gaben, Formatnamen-Wache, zwei Profile), 9 Fälle der Abbildung (`GebaeudeImportHuelleTests`) |

**Arbeitsentscheide der Welle.**

1. **Bauweise nur als Bauart.** Der Gebäudeeditor rechnet im Modus Neu die Bauweise aus Nutzfläche und
   Bauart nach (`GebaeudeArbeitsstand.Laden`/`Ableiten`); ein freier Wert hielte nicht. Der Import setzt
   deshalb die Bauart, eingerastet über `Gebaeudebauweise.BauartAusBauweise`; der Wert aus den Schichten steht
   im Beleg (Probenhaus: 33,8 Wh/(m²K) → „schwer"). Die Zeilenangabe im Umsetzungskonzept 3.4 zur
   Nachführung ist veraltet.
2. **Die gesamte Fensterfläche ist abgeleitet** — der Editor bildet sie aus den vier Himmelsrichtungen; sie wird
   angezeigt, aber nicht übernommen.
3. **OK ohne Schreibdelegat ist weich gesperrt** (Grund am Knopf, Meldung beim Klick) statt versteckt: Jeder
   Dialog trägt OK und Abbrechen, und ein OK, das ohne Übernahme schlösse, behauptete etwas.
4. **KI und Hilfe:** Der Dialog steht wie der Katalogimport als Ausnahme „Import" in `KiDialogAusnahmen`; die
   Hilfe nutzt vorerst den Schlüssel des Gebäudeeditors — eine eigene Hilfeseite kommt mit der Anbindung.

**Nachweis.** Build Kern-Filter und Windows-Schale 0 Fehler; volles Test-Gate grün nach dem Zusammenführen mit
origin (EPOS.Kern.Tests 6 448, EPOS.UI.Tests 6 121, SpeicherEngine 386, SpeicherPlanung 27 und 1 fremd übersprungen, KiKern 549); kein Referenzlauf nötig (kein Rechenweg berührt, `Bauteilreduktion` wird nur gerufen).

**Offen.** Belegwerte erscheinen invariant formatiert (Punkt als Dezimalzeichen auch im deutschen Text) —
die Anzeige formatiert sie mit der Anbindungswelle in der Anzeigekultur. Die Anbindung braucht: Knopf im
Gebäudedialog, Überlagerung, vorbelegten Gebäudeeditor im Modus Neu, Übernahme der Herkunft nach dem OK des
Editors (die neue Gebäude-Id entsteht erst dort), eigene Hilfeseite und Logbuch-Eintrag.

## 3 G4a Welle 1 — xBIM-Paket, Lizenzhinweisseite, IFC-Leser (25.09.2026)

In einem eigenen Worktree parallel zu G4c Welle 2 gebaut und danach zusammengeführt; ohne Oberfläche und
ohne Schemaschritt.

| Teil | Stand |
|---|---|
| Paket (G4-1) | eine Zeile `Xbim.IO.MemoryModel` 6.1.605 in `Directory.Packages.props`, `PackageReference` nur am Kern; transitiv allein `Xbim.Common` und die drei Schemapakete (6.1.605) sowie drei `Microsoft.Extensions`-Pakete — kein Esent, kein Geometriekern (E3, ADR-003); die fünf xBIM-Assemblies sind in der Ausgabe der Schale zusammen rund 11,1 MB groß; der Kern baut weiter ohne Windows-Bindung (CA1416 = 0) |
| Lizenzhinweisseite (U10) | neu `Setup/Vorlage/Lizenzhinweise.txt`: je ausgelieferter Fremdbibliothek Fassung, SPDX-Lizenz, Copyright und Quelltextverweis, offline aus den `.nuspec` der ausgelieferten Fassung; für xBIM der Verweis nach CDDL-1.0 Abschnitt 3.1 samt Stand im Quelltextarchiv; `Setup/EPOS-Plan.iss` legt die Seite nach `{app}` und bricht ohne sie mit `#error` ab; Wache `LizenzhinweiseWacheTests` (jedes direkt referenzierte Paket der ausgelieferten Projekte steht auf der Seite) |
| Leser `EPOS.Kern/Allgemein/Import/Ifc/` | `IfcLeser` (STEP, ifcXML, `.ifczip` am Inhalt erkannt; beim Zip die entpackte Größe laut Verzeichnis; Schema aus dem Kopf — IFC2X3, IFC4, IFC4X3 angenommen, IFC4X1 benannt abgelehnt), `IfcAbbildBauer` (Gebäude, Räume, Raumgrenzen, Bauteile, Öffnungen, Schichten in das gemeinsame Abbild), `IfcEigenschaften` (Psets und Mengen selbst, Eigenschaftsmengen aufgelöst, Vorkommnis vor Typ), `IfcEinheiten` (jede Größenart mit eigenem Prefix), `IfcPlatzierung` (Azimut ohne Geometriekern über die Platzierungskette, Außenseite über die Raumgrenze, TrueNorth bzw. MapConversion), `IfcProtokoll` (eigene Protokollsenke je Lauf, zählt beide Verlustkanäle als `FehlendeEntitaeten`), `IfcImportProfil` (50 MB Windows / 20 MB iOS, U11), dazu formatfrei `Baujahrregel` |
| Gemeinsame Aggregation | Klasse aus dem Baujahr (A…H), wenn keine gewählt ist; Raumhöhe als flächengewichtetes Mittel; `NetSideArea` als Rückfall des Fensterabzugs (U14); `FehlendeEntitaeten` bis in die Quelle; gbXML-Fälle weiter grün |
| Importproben | acht Dateien unter `Referenzlaeufe/Importproben/`: sieben erzeugt eine Testhilfe mit xBIM deterministisch (IFC4- und IFC2X3-Haus, `.ifczip`, IFC4X1-Kopf, zwei Gebäude, ohne Mengen, MapConversion), eine ist von Hand geschrieben (Entitätenverlust, 1,7 KB); eingetragen in `LIESMICH_Importproben.md`. Die KIT-Datei `AC20-FZK-Haus.ifc` ist **nicht** aufgenommen — sie herunterzuladen entscheidet der Anwender |
| Tests | 43 Fälle (`IfcImportTests`, `IfcProbenTests`, darunter die 14 Unit-Tests ohne Datei aus Umsetzungskonzept 3.7), die Lizenzwache, der Probenerzeuger als benannt übersprungener Fall |

**Befund zu xBIM.** `OpenReadStep21(Stream, ILogger, …)` reicht den übergebenen Logger nicht an den Parser
weiter, sondern nimmt ihn aus `XbimServices`. Der Leser öffnet das Modell deshalb über den Konstruktor mit
eigener Logger-Fabrik und lädt dann; `XbimServices` wird nicht konfiguriert (kein globaler Zustand).

**Abweichungen von Umsetzungskonzept 3.4/3.5** (im Quelltext belegt): ein Außenbauteil mit mehreren
anliegenden Räumen bekommt den ersten beheizten Raum als Nachbarn; ohne jede Angabe gilt ein Dach als außen,
eine Bodenplatte als erdberührt; die Außenplatte `FLOOR` ist Boden oder Decke nach der Geschosslage;
zusätzlich wird der Mengensatzname `Qto_<Klasse>Quantities` angenommen; Fenster- und Türfläche mit drittem
Rückfall `OverallWidth × OverallHeight`; U14 ohne `NetSideArea` bleibt wie in G4c ein Fehler; mehrschalige
Wände nur mit Warnung. Befund N 4.6 enthält einen Vorzeichenfehler beim TrueNorth-Beispiel; der Test prüft
richtig. Noch nicht gebaut: die Vorgabe-Rückfälle für Raumhöhe, Dach- und Grundfläche, Bauweise aus
Schichten beim IFC-Weg, Stoffwerte aus IFC2X3.

**Nachweis.** Build Kern-Filter und Windows-Schale 0 Fehler, Kern ohne Windows-Bindung 0 Fehler (CA1416 = 0); volles Test-Gate nach dem Zusammenführen mit G4c Welle 2 grün (EPOS.Kern.Tests 6 509 und 1 benannt übersprungen, EPOS.UI.Tests 6 121, SpeicherEngine 386, SpeicherPlanung 27 und 1 fremd übersprungen, KiKern 549); nach dem Zusammenführen mit origin (Schemastand 137) gezielt 444 Import-, Lizenz- und Wachenfälle grün; SqlDialektPruefer 1 895 SQL-Texte, 0 Fundstellen; Auslieferungsvorlage.Tests 32/32; Referenzlauf aller dreizehn Projekte gegen R14 13/13 PASS, byte-gleich (außer protokoll.txt). CI zu G4c Welle 2: kern.yml 36075706840 grün.

**Offen für G4a.** Das IFC-Profil im Zuordnungsdialog und in der Hülle; die iOS-Dateifilter `.ifcxml` und
`.ifczip`; die iOS-Größengrenze messen und der Trimming-Nachweis (G4-8) im einen iOS-Lauf, **nur nach
Rückfrage beim Anwender zur Abnahme von G4a** (E38); die Persistenz über S-F; die Rückfälle und die Bauweise
aus Schichten.

**CI zu G4a Welle 1.** `kern.yml` 36078014135 grün — das xBIM-Paket baut und testet auch auf ubuntu.

## 4 G4c Welle 3 — Persistenz der Zuordnung, Schemaschritt 138 (S-F, 25.09.2026)

In einem eigenen Worktree gebaut. Die Nummer war mit der Anlagenkopplung abgestimmt („wer zuerst pusht");
deren Welle 4 hat 135–137 zuerst gepusht, S-F ist deshalb **Schritt 138**. Ergebnisneutral: zwei leere
Tabellen, kein Rechenweg liest sie, Import nur auf Zuruf.

| Teil | Stand |
|---|---|
| `ImportzuordnungSchema` (eine Quelle für Migration, Testdatenbankschema, Testvorrichtung) | `Tab_Importquelle` (Format, Dateiname ohne Pfad, SHA-256, Größe, Schemastand, Zeitpunkt, Programmfassung, Zonenregel, `FehlendeEntitaeten`; `ON DELETE CASCADE` auf `Tab_Gebaeude`) und `Tab_Importzuordnung` (fünf nullbare Zielverweise auf Gebäude, Zone, Bauteil, Aufbau, Baustoff mit `CHECK` genau eines Ziels, `Quellkennung` ≤ 64, `Quelltyp`; Indizes `(ID_Importquelle)` und `(Quellkennung)`), beide `STRICT` |
| Migration | Schritt 138 in `SchemaMigration` mit den vier Stücken des `SCHRITTE_SQLITE`-Eintrags; `SchemaStand.Zielversion` zeigt auf die Schrittkonstante; `DbWerte.IMPORT_FORMAT_*`, `SchemaKatalog`-Konstanten |
| Testdatenbank | auf 138 über `Werkzeuge/Testdatenbankschema`; Zellvergleich gegen die Fassung 137: nur `SchemaVersion` geändert, neu die zwei leeren Tabellen und zwei Indizes; `integrity_check` ok, `foreign_key_check` leer, 143 STRICT-Tabellen; LFS-Zeiger `3f5c892d…` |
| Registerpflege | `ProjektDuplizierenCtrl`: `FK_MAP` um `ID_Importquelle` und das bisher fehlende `ID_Bauteil`, `ID_Zone` der Paarung über `FK_OVERRIDE` (in `FK_MAP` steht `ID_Zone` für die Trinkwarmwasserzone), `KINDER` zweistufig Quelle → Zuordnung; Duplizieren und Projekttransfer nehmen beides mit (Tests). Reduzierskript: zwei `DELETE`-Zeilen, Eltern zuerst, samt Gegenprobe. Auslieferungsvorlage: Prüfregel „Importablage leer" (auch eine fehlende Tabelle ist ein Befund), STRICT-Zahl 141 → 143 |
| `GebaeudeImportCtrl` | `SchreibeHerkunft` (eine Quelle und je Paarung eine Zeile in einer Transaktion; im Vorgang des Aufrufers ein Sicherungspunkt; Kennungen über `Quellkennung.Kuerzen`), `LesenQuellen`, `LesenZuordnungen`, `FindeZuordnung` (Weg des Round-Trips), `SchonImportiert` (zweiter Import derselben Datei), `Einzonenpaarungen` (in G4 nur Gebäude ↔ `Building/@id` bzw. `IfcBuilding`); Modelle `ImportquelleModel`, `ImportzuordnungModel` |
| Tests | Probe 14 (Wiederfinden über die Quellkennung; gewöhnliches Speichern lässt Quelle und Paarung stehen; Löschen des Gebäudes bzw. Projekts leert beide), Probe 24 (überlange Kennung gekürzt in der Tabelle), Duplizieren, Transfer, IFC-Herkunft mit der G4a-Probe |

**Arbeitsentscheid — Kaskade auf die Zielverweise (Abweichung von Softwarearchitektur 2.2 und
Datenaustauschkonzept 7.2, „ohne Kaskade").** Die Fremdschlüssel sind im Betrieb scharf; ohne Löschregel
scheiterte das Entfernen einer importierten Zone, eines Bauteils, Aufbaus oder Baustoffs am Verweis, und
`SET NULL` verletzte den `CHECK`. Die Begründung der Papiere — ein Speicherweg, der löscht und neu anlegt,
räumte die Paarung bei jedem Speichern ab — trägt nicht mehr: `GebaeudeZonenCtrl` gleicht über die Ids ab
(A6), und die Messung A1 der Sitzung G3 hat gezeigt, dass kein gewöhnlicher Speicherweg ein Gebäude löscht und
neu anlegt; eine Kaskadenrettung in `WizardCtrl` braucht es deshalb nicht. Die fünf Zielverweise tragen
`ON DELETE CASCADE` — eine Paarung ohne Ziel hat keine Bedeutung. Die Tests belegen alle drei Fälle (Ziel
entfernen: Speichern gelingt, genau diese Paarung ist weg; gewöhnliches Speichern: alles bleibt; Gebäude
oder Projekt löschen: beide Tabellen leer). G3 hat bestätigt, dass das für `SpeichernJeGebaeude` passt.

**Nebenbefund.** Testaufbauten, die eine Datenbank vor S-C über `DROP TABLE` nachbilden, räumen jetzt zuerst
die S-F-Tabellen ab (die G3-Idempotenzprobe und ein Fall der Anlagenkopplung, angepasst). Im Betrieb ohne
Wirkung.

**Nachweis** (auf dem zusammengeführten Endstand): Build Kern-Filter und Windows-Schale 0 Fehler; volles
Test-Gate grün (EPOS.Kern.Tests 6 574 und 1 benannt übersprungen, EPOS.UI.Tests 6 121, SpeicherEngine 386,
SpeicherPlanung 27 und 1 fremd übersprungen, KiKern 549); `Auslieferungsvorlage.Tests` 34/34;
`SqlDialektPruefer` 1 905 SQL-Texte, 0 Fundstellen; Reduzier-Probe grün; Referenzlauf aller dreizehn Projekte
13/13 PASS, 394/394 CSV byte-gleich.

**Offen.** Die Anbindung schreibt die Herkunft beim Übernehmen im selben Vorgang wie die Gebäudezeile
(`SchreibeHerkunft`) und zeigt „dieselbe Datei schon importiert" (`SchonImportiert`). Die Aggregation erzeugt
weiter Paarungen für Räume, Flächen und Öffnungen; der Einzonenweg schreibt nur die Gebäudepaarung — die
übrigen gehören zu G6c. Für G6c zu merken: Die Zielspalten der Zuordnung tragen keinen eigenen Index.

## 5 G4a Welle 2 — Rückfälle, Bauart aus Schichten, iOS-Dateifilter (25.09.2026)

Im Worktree des IFC-Wegs gebaut, ohne Oberfläche und ohne Schemaschritt.

| Teil | Stand |
|---|---|
| Vorgabe-Rückfälle (Umsetzungskonzept 3.4) | in der gemeinsamen Aggregation, **eingeschaltet nur über das IFC-Profil** (`RueckfallRaumhoeheM`, `FlaechenRueckfaelle`) — gbXML zählt weiter wie in G4c („leer = Wert des Gebäudes"). Raumhöhe ohne Angabe → 2,5 m; Dachfläche ohne Mengen → Bruttogrundfläche des obersten beheizten Geschosses; Grundfläche → Nutzfläche ÷ Zahl der beheizten Geschosse (ohne jedes Bodenbauteil auch die Randbedingung „Erdreich" als Vorgabe); Sonstige → 0; jede mit Herkunft `Vorgabe` und Beleg; eine Teilsumme bleibt stehen und wird gelb. Volumen gegen Nutzfläche × Raumhöhe (> 20 %) warnt schon in der Zuordnung |
| Bauart aus Schichten beim IFC-Weg | dieselbe Regel wie G4c Welle 2; λ und c aus `Pset_MaterialThermal`, ρ aus `Pset_MaterialCommon`, Werte ≤ 0 als Fehlstelle (`STOFFWERT_NULL`, dann Vorgabe); die raumseitige Schicht aus `IfcMaterialLayerSetUsage` und der Raumseite (Wand über die Raumgrenze, Platte über ihre Art) statt der gbXML-Annahme „erste Schicht außen" — sonst Annahme mit Meldung `SCHICHTFOLGE_ANGENOMMEN` (am Schichtenhaus 55,4 statt 28,0 Wh/(m²K), „schwer" statt „leicht") |
| IFC2X3-Stoffwerte | über die Schnittstellen von xBIM 6.1.605 nicht verlässlich lesbar (`IfcThermalMaterialProperties` liefert nichts, `IfcExtendedMaterialProperties` wirft `MissingMethodException` — der Absturz ist abgefangen); benannt „nicht gelesen" (`STOFFWERTE_NICHT_GELESEN`), U-Wert und Bauart dieser Schichten fallen auf die Vorgabe; ein Befundtest hält das Verhalten fest. Ein Lesen über die EXPRESS-Metadaten wäre eine dritte Schemaverzweigung über Umsetzungskonzept 3.5 Nr. 7 hinaus und ist nicht gebaut |
| iOS-Dateifilter | `EPOS.iOS/Dienste/Dateifilter.cs`: `.ifcxml` → `public.xml`, `.ifczip` → `public.zip-archive`, `.gbxml` → `public.xml`; `.ifc` bleibt beim Rückfall `public.data` mit Kommentar; ein Test liest die Tabelle aus dem Quelltext (die Schale selbst baut nur auf macOS — geprüft wird sie im einen iOS-Lauf zur Abnahme von G4a) |
| Proben und Tests | vier neue Proben (`ifc4_schichten.ifc`, `ifc4_schichten_nullwerte.ifc`, `ifc2x3_schichten.ifc`, `ifc4_rueckfaelle.ifc`), die übrigen byte-gleich; 13 Tests (`IfcImportWelle2Tests`); 9 Texte in beiden Sprachen |

**Offen.** Steht an einem Dach nur ein U-Wert, aber keine Fläche, nimmt die Gruppe die Vorgabe der
Baualtersklasse statt dieses U-Werts.

**Nachweis.** Nach dem Zusammenführen mit origin (Schemastand 138): Build Kern-Filter und Windows-Schale 0 Fehler; volles Test-Gate grün (EPOS.Kern.Tests 6 587 und 1 benannt übersprungen, EPOS.UI.Tests 6 121, SpeicherEngine 386, SpeicherPlanung 27 und 1 fremd übersprungen, KiKern 549); SqlDialektPruefer 1 905 SQL-Texte, 0 Fundstellen; Referenzlauf aller dreizehn Projekte 13/13 PASS, byte-gleich (außer protokoll.txt). CI zu G4c Welle 3 (Schritt 138): kern.yml 36080790745 grün.

## 6 G4 Welle 4 — Anbindung im Gebäudedialog (25.09.2026)

Ohne Schemaschritt, ohne Rechenweg; berührt ist der Speicherweg der Gebäudeliste. Der Ablauf ist ein
Arbeitsentscheid des Orchestrators (unten, Nr. 1).

| Teil | Stand |
|---|---|
| Einstieg (A17) | EIN Knopf „Importieren (gbXML, IFC)…" in der Leiste unter dem Katalog des `GebaeudeDialog`, neben „Gebäude in DB neu…"; Delegat `ImportGaben` (`GebaeudeImportweg` je Klick), ohne ihn kein Knopf, kein Menüpunkt, kein Maskenschlüssel. Der Zuordnungsdialog steht als fünfte Überlagerung ohne eigenen Kopf (Muster `ImportUeberlagerung`); Esc des Gebäudedialogs wartet, solange sie steht. `GebaeudeFenster` wünscht `Fenstermass.MitUeberlagerung` (1 180 × 760) |
| Format | EINE Dateiwahl mit dem gemeinsamen Filter `GebaeudeImportProfil.DATEIFILTER_ALLE`; das Profil folgt der Endung (`FuerDatei`: `.ifc`/`.ifcxml`/`.ifczip` → IFC, `.xml`/`.gbxml` → gbXML), sonst die benannte Ablehnung „Dateiart nicht unterstützt" (`GIMP_DLG_DATEIART`). `GebaeudeImportHuelle()` entscheidet nach der Datei, `GebaeudeImportHuelle(profil)` behält ein festes Profil (die Tests der Welle 2 laufen darüber weiter); die Größengrenze gilt je Profil und Plattform, die Grenzzeile nennt beide („gbXML 25 MB · IFC 50 MB"). Befund: `IfcImportProfil` überschrieb `GrenzeFuerPlattform` nicht — unter iOS hätte die Hülle 50 statt 20 MB belegt; nachgetragen samt Test |
| Übernahme | Der Wirt liefert `Uebernehmen`: derselbe Satz wie an der Prüfung; ein im Katalog vergebener Name ist die benannte Absage (der Dialog bleibt offen); sonst `GebaeudeImportHuelle.Vorbelegung` = `NachKatalogdaten(GebaeudeKatalogHuelle.AusModell(new GebaeudeModel()), ergebnis)` und die ausstehende Herkunft (`Herkunft`: Quelle + `Einzonenpaarungen`). Danach öffnet der Katalogeditor im Modus Neu, vorbelegt, mit der Herleitungszeile „Vorbelegt aus dem Import: Datei …, Format …" und dem Überlagerungstitel „Gebäude in DB neu…" |
| Editor (für den Merge mit AK1) | `GebaeudeKatalogHuelle.Gaben(bezeichner, modus, GebaeudeVorbelegung vorbelegung = null)`: der bisherige Rumpf heißt unverändert `Grundgaben`; mit Vorbelegung wird nur `Daten` ersetzt und `Vorbelegung` ergänzt, ohne sie ist der Satz dasselbe Objekt. `GebaeudeKatalogDialog`: ein Parameter `Vorbelegung` und eine `Herleitungszeile` über den Reitern. `GebaeudeArbeitsstand`, `GebaeudeStammblattFelder` und `AusModell`/`NachModell` sind nicht berührt |
| Projektliste | Speichert der Editor (die Hülle merkt sich über einen Umschlag des `Speichern`-Delegaten nur den angelegten Namen), nimmt der Gebäudedialog die neue Zeile selbst auf — über `Aufnehmen` wie „In das Projekt übernehmen", markiert, mit Hinweis `GEB_MSG_IMPORT_AUFGENOMMEN` — und hängt den undurchsichtigen `GebaeudeProjektZeile.Herkunftsschluessel` an. Quelle und Paarungen bleiben in `GebaeudeHuelle.Gaben` (Wörterbuch je Schlüssel); `NachModell` legt sie an `Z_ProjGebModel.Importherkunft` (`internal`, Typ `GebaeudeImportHerkunft`). Ein Modell, das seine Herkunft noch trägt (Assistent: dieselbe Liste über mehrere Seitenbesuche), bekommt beim Neuaufbau wieder einen Schlüssel. Abbrechen im Editor: keine Zeile, die Herkunft verfällt |
| Speicherweg | `WizardCtrl.GebaeudeZuordnungAnlegen` schreibt die Herkunft NACH `CopyFromStamm` an die neue Projektkopie (`SchreibeHerkunft` im Vorgang des Aufrufers, als Sicherungspunkt). Scheitert sie, gibt der Schritt 0 zurück, `Speichere_Projekt_Gebaeudeliste` rollt den ganzen Vorgang zurück und nennt den Grund (`GEB_MSG_LISTE_NICHT_GESPEICHERT` + `WizardCtrl.Herkunftsfehler`). Eine bleibende Zeile kommt im Abgleich nicht in den Zweig „neu" und schreibt keine Herkunft neu |
| Schon importiert | `GebaeudeImportCtrl.ImporteImProjekt(idProjekt, hash)` (Projektkopie, Name, Zeitpunkt; jüngster zuerst); die Hülle nennt nach dem Lesen das jüngste Gebäude in einer leisen Zeile des Zuordnungsdialogs (`GebaeudeLesestand.SchonImportiert`, Zeitpunkt in der Anzeigekultur) — kein Sperren |
| Belegkultur | `GebaeudeZuordnungsModell.AnzeigeWert` in `BelegText` und `MeldungText`: eine invariant geschriebene Dezimalzahl erscheint in der Anzeigekultur (de-DE „18,37"), Ganzzahlen, Kennungen und Texte bleiben; der Versionswert einer Datei (`VERSION_UNBEKANNT`, `SCHEMA_UNBEKANNT`) bleibt, wie er steht. Gespeichert und verglichen wird weiter invariant; `PruefMeldung` ist nicht berührt |
| Hilfe, KI | Wiki-Quelle `Projekte/Wiki/Programm Dokumentation - Gebäudeimport.wiki` (18 Anker, Ziel des Hilfeknopfs `zuordnung`); `help_mapping.txt`: `Form_GebaeudeImport.btn_Help = Gebäudeimport#zuordnung`; beide Profile tragen `GebaeudeImportProfil.HILFE_ZUORDNUNG`, der Dialog denselben Schlüssel als Vorgabe seines Parameters `HilfeSchluessel` (das Profil gewinnt); `KiDialogAusnahmen` nennt ihn. Die Seite Gebäude verweist mit dem neuen Anker `import`; Upload-Liste und Logbuch-Satz in `Wiki_Update_2026-09-26.md`. Der Knopf braucht keine KI-Feldanmeldung |
| Texte | sieben Schlüssel in beiden Sprachen (`GEB_BTN_IMPORT`, `GEB_BTN_IMPORT_HINWEIS`, `GEB_MSG_IMPORT_AUFGENOMMEN`, `GIMP_DLG_DATEIART`, `GIMP_DLG_SCHON_IMPORTIERT`, `GIMP_VORBELEGT`, `GIMP_ZONENREGEL_Z5` — der Kopf zeigte für IFC bisher den nackten Schlüssel „Z5"); `ResourceDesigner` neu erzeugt, wiederholbar |
| Tests | Kern: `GebaeudeImportProfilwahlTests` (13), `GebaeudeImportAnzeigekulturTests` (12, de-DE und en-US), `GebaeudeImportAnbindungTests` (5: Liste mit Herkunft, ohne Herkunft, Rücklauf beim Fehler, bleibende Zeile, Abfrage je Projekt), `GebaeudeImportDialogwegTests` (2: der ganze Weg über die Hüllen, vergebener Name), `GebaeudeImportHuelleTests` +5 (Profil nach Endung, Herkunft, Vorbelegung, Zeitpunkt); bunit `GebaeudeDialogImportTests` (5: Knopf nur mit Delegat, Überlagerung ohne Kopf und Abbrechen, vorbelegter Editor, neue Zeile mit Schlüssel, ohne Speichern keine Zeile) |

**Arbeitsentscheide der Welle.**

1. **Der Import legt ein NEUES Gebäude an — über den vorbelegten Katalogeditor** (Orchestrator), nicht
   durch Überschreiben einer vorhandenen Projektkopie: Es gibt keinen Editor für Projektkopien, eine
   abweichende Kopie wäre für den Anwender unsichtbar, und der Katalogsatz zeigte andere Werte, als das
   Projekt rechnet.
2. **Bricht der Anwender den Gebäudedialog ab, bleibt der Katalogsatz** (wie nach jedem Speichern im
   Editor), und es entstehen weder Projektzeile noch Herkunft. Der Satz lässt sich später über „In das
   Projekt übernehmen" aufnehmen — dann ohne Herkunft, denn die gehört zum Lauf, nicht zum Katalog.
3. **Der vergebene Name wird schon am OK des Zuordnungsdialogs abgelehnt** und nicht erst im Editor: Dort
   stünde die Absage nach allen Handänderungen der Zuordnung.
4. **Der Hilfeschlüssel steht auch als Vorgabe im Dialog** (`HilfeSchluessel`): Die Wache
   `KiMaskenabdeckungWacheTests` verlangt den Schlüssel einer Ausnahme in deren Datei; der Wert des
   Profils gewinnt weiterhin.
5. **Der Hinweis „schon importiert" nennt das jüngste Gebäude** mit Zeitpunkt; weitere Treffer nennt er
   nicht, gesperrt wird nichts.

**Nachweis.** Build Kern-Filter (Release) und Windows-Schale (Debug x64) 0 Fehler, keine neue Warnung in den
berührten Dateien; volles Test-Gate grün (EPOS.Kern.Tests 6 611 und 1 benannt übersprungen, EPOS.UI.Tests
6 126, SpeicherEngine 386, SpeicherPlanung 27 und 1 fremd übersprungen, KiKern 549); `ResourceDesigner`
wiederholbar (+0); `SqlDialektPruefer` 1 906 SQL-Texte, 0 Fundstellen; Tabuwort-Muster der Wurzel-`CLAUDE.md`
auf beiden Wiki-Quellen 0 Treffer; Referenzlauf aller dreizehn Projekte gegen `2026-09-24_R14_Kaelteerzeuger`
**13/13 PASS**, 394/394 CSV byte-gleich (außer `protokoll.txt`) — der Speicherweg der Gebäudeliste ist
berührt, der Rechenweg nicht.

**Offen.** Die Sichtabnahme unter Windows (Knopf, Überlagerung, vorbelegter Editor, neue Zeile, OK); die
iOS-Dateiwahl bietet `.ifcxml` und `.ifczip` über `public.data` an (die Tabelle in `EPOS.iOS/Dienste/Dateifilter.cs`
kennt sie nicht) — zu prüfen mit dem einen iOS-Lauf zur Abnahme von G4a, nur nach Rückfrage (E38).

**Nachweis nach dem Zusammenführen.** Welle 4 zusammen mit G4a Welle 2 (origin): Build Kern-Filter und Windows-Schale 0 Fehler; volles Test-Gate grün (EPOS.Kern.Tests 6 624 und 1 benannt übersprungen, EPOS.UI.Tests 6 126, SpeicherEngine 386, SpeicherPlanung 27 und 1 fremd übersprungen, KiKern 549); SqlDialektPruefer 1 906 SQL-Texte, 0 Fundstellen; Referenzlauf aller dreizehn Projekte 13/13 PASS, byte-gleich (außer protokoll.txt). CI zu G4a Welle 2: kern.yml 36082656638 grün.

**CI zu Welle 4.** `kern.yml` 36086864323 grün.

**Papiere.** Umsetzungskonzept, Datenaustauschkonzept und Softwarearchitektur tragen je einen Nachzug
„Umsetzung G4" (Namen, Übernahmeweg über den vorbelegten Editor und die Gebäudeliste, Schritt 138 mit Kaskade
auf die Zielverweise, Arbeitsentscheide dieses Protokolls, eigene Proben statt der KIT-Datei, offene Punkte).

## 7 G4a Welle 3 — Schemaschritt 139 „Baujahr" (25.09.2026)

Mit den Sitzungen Anlagenkopplung und G3 abgestimmt; ergebnisneutral (nichts gesät, kein Rechenweg liest die
Spalte).

| Teil | Stand |
|---|---|
| Schema | `BaujahrSchema` (Schrittkonstante 139): Spalte `Baujahr INTEGER` mit `CHECK (Baujahr IS NULL OR Baujahr BETWEEN 1500 AND 2100)` an `Tab_Gebaeude` und `Tab_Gebaeude_STAMM`; fünfter Durchgang der Sicht `Abfrage_Projektgebaeude` (98 → 99 Spalten) nach M3, KU-S1, AK-S1 und KAK-S1, in Migration, Testdatenbankschema und Testvorrichtung zuletzt |
| Testdatenbank | 138 → 139; Zellvergleich: nur `SchemaVersion`, die neue Spalte (überall NULL) und der Sichttext; `integrity_check` ok, `foreign_key_check` leer; LFS-Zeiger `f700e81e…`; Nachtrag in `Referenzlaeufe/LIESMICH.md` |
| Kopierwege | `GebaeudeStammCtrl` (Insert, Overwrite, CopyFromStamm NULL-erhaltend), Namensleser `ProjektGebaeudeCtrl`, beide Modelle, Duplizieren und Transfer |
| Oberfläche | Ganzzahlfeld „Baujahr" neben der Baualtersklasse im Katalogeditor und in den Kenndaten der Gebäudeverwaltung (`GebaeudeAdminDialog`), Prüfung 1500–2100 im geteilten Arbeitsstand, Lesemodus und Vergleich mit zwei Zeilen; die Klappliste der Klasse heißt jetzt „Baualtersklasse" (`GEBK_LBL_BAUALTERSKLASSE`, `GEBA_LBL_BAUALTERSKLASSE`), „Baujahr" gehört dem Jahresfeld; Feld `baujahr` im Assistenten beider Gebäudemasken; Glossar § 13 um Baualtersklasse und Baujahr ergänzt |
| Import | Zielfeld „Baujahr" hinter der Klasse; IFC aus `Pset_BuildingCommon.YearOfConstruction` (Herkunft Ifc, Beleg mit dem gelesenen Text), gbXML leer; `NachKatalogdaten` setzt das Feld; die Prüfung am OK weist ein Jahr außerhalb des Bereichs ab |
| Wiki-Quellen | „Gebäude" und „Gebäudeimport" nachgezogen (gegen das Verbotsmuster gegengelesen, kein Logbuch-Satz, nicht hochgeladen) |

**Bewusst unverändert:** Die Filter „Baujahr" im Gebäudedialog und in der Gebäudeverwaltung sowie das Feld im
Wohnflächendialog meinen weiter die Klasse (eigene Schlüssel); eine Umbenennung dort wäre ein kleiner
Folgeauftrag.

**Nachweis.** Build Kern-Filter und Windows-Schale 0 Fehler; volles Test-Gate grün (EPOS.Kern.Tests 6 690 und 1
benannt übersprungen, EPOS.UI.Tests 6 154, SpeicherEngine 386, SpeicherPlanung 27 und 1 fremd übersprungen,
KiKern 549); `Auslieferungsvorlage.Tests` 34/34; `SqlDialektPruefer` 1 910 SQL-Texte, 0 Fundstellen; Referenzlauf
aller dreizehn Projekte 13/13 PASS, 394/394 CSV byte-gleich.

## 8 Stand der Stufe G4 nach Welle 7

**G4c (gbXML-Import) ist gebaut und angebunden;** offen ist allein die Windows-Sichtabnahme durch den Anwender.
**G4a (IFC-Import) ist gebaut und angebunden;** offen sind die Windows-Sichtabnahme und — nach E38 — der eine
iOS-Lauf (Gerätebau mit Trimming-Nachweis, gemessene iOS-Größengrenzen, Dateiwahl mit `.ifcxml`/`.ifczip`),
ausschließlich nach Rückfrage beim Anwender zur Abnahme von G4a. Die Aufnahme der KIT-Datei `AC20-FZK-Haus.ifc`
als weitere Importprobe entscheidet der Anwender (Download). G4b folgt nach G3 und nachdem G4a im Feld war.

**CI zu Welle 7.** `kern.yml` 36091515983 grün.

## 9 G4-8 — der eine iOS-Lauf vorbereitet, IFC-Leser linear (25.09.2026)

**Anwenderentscheide (25.09.2026, zur Abnahme von G4a nach E38 gefragt):** den iOS-Lauf **vorbereiten und
dann genau einmal starten**; die KIT-Datei `AC20-FZK-Haus.ifc` **nicht** herunterladen — die selbst erzeugten
Proben genügen.

| Teil | Stand |
|---|---|
| Messlogik im Kern | `Importmessung` und `ImportmessungProben` (plattformfrei, unter Windows getestet): fährt den ganzen Importweg für eine Datei und liefert invariante Zeilen `IMPORTPROBE`/`IMPORTMESSUNG`/`IMPORTDIAGNOSE` (Format, Größe, Gebäude, Nutzfläche, Meldungen je Stufe, verlorene Entitäten, Dauer, Speicherzuwachs und Faktor „MB Speicher je MB Datei", Typzahl der drei xBIM-Schemata gegen den ungetrimmten Stand); synthetische Großfälle im Speicher erzeugt (gbXML 2 / 8 MB und knapp unter 10 MB, IFC 2 / 8 MB und knapp unter 20 MB) |
| iOS-Schale | `EPOS.iOS/Pruefung/Importprobe.cs`, `Prozessspeicher.cs` (Prozessspeicher über `task_info`, sonst benannter Rückfall), ein gekapselter Block im Prüflauf hinter dem Rechennachweis (Schalter `EPOS_PRUEFLAUF_IMPORT`), vier kleine Proben als App-Ressourcen (`-p:Importproben=true`), eine Zeile mit den Einträgen der Dateifilter-Tabelle |
| `ios.yml` | Eingang `abnahme_g4a` (Vorgabe aus — ohne ihn läuft der Workflow wie zuvor): hinter dem unveränderten Basisnachweis ein Release-Simulatorbau (AOT-only, `TrimMode=partial`, ohne LLVM) mit Paketvergleich, Neuinstallation und Prüfmodus samt Importprobe, Auswertung der Importzeilen, Rechennachweis 1030 der getrimmten App (Information); parallel ein Job `geraetebau` (`ios-arm64`, Release, ohne Signatur): IL-Warnungen, Paket- und AOT-Größen; jeder neue Schritt `continue-on-error`, Artefakte `g4a-simulator` und `g4a-geraetebau` |
| `TrimmerRootDescriptor` | bewusst nicht angelegt (Umsetzungskonzept 3.6: erst, wenn der Lauf fehlende Typen zeigt); der Prüfmodus protokolliert dafür jede Ausnahme des IFC-Wegs vollständig |

**Befund und Korrektur: Der IFC-Leser wuchs quadratisch.** Die Vorbereitung hat gemessen, dass synthetische
IFC-Dateien mit 2 / 8 MB etwa 6 s / 60–130 s brauchten und 20 MB nicht in fünf Minuten fertig wurden:
`IfcAbbildBauer` und `IfcEigenschaften` fragten jede Rückbeziehung (`IsDefinedBy`, `IsTypedBy`,
`HasAssociations`, `IsDecomposedBy`, `ContainsElements`, `HasOpenings`, `HasFillings`, `HasProperties`)
unmittelbar bei xBIM ab, und jede Abfrage lief über alle Beziehungen ihres Typs. Neu baut `IfcRueckbezuege`
je Modell einmal einen Index je Beziehungsart (dieselbe Schnittstelle, dieselbe Reihenfolge); der Leser liest
nur noch daraus. Danach: 2 MB 0,4 s, 8 MB 1,4 s, 20 MB 1,6 s, **50 MB 4,5 s** (Windows, Release); der
Speicherbedarf ist unverändert (50 MB Datei → 710 MB verwalteter Zuwachs, Faktor 13,4 — für die iOS-Grenze
belangvoll, gemessen wird er im iOS-Lauf). Der eigene Zwischenspeicher von xBIM (`BeginInverseCaching`) ist
kein Ersatz: Er liefert für Rückbeziehungen, die das Schema der Datei nicht führt, still eine leere Menge — an
der IFC2X3-Probe verschwänden alle neun Stoffsätze samt der Meldung „nicht gelesen" (Befundtest). Tests:
Gleichheit des Index mit xBIM für alle acht Rückbeziehungen an sieben Proben, Quelltextwache gegen direkte
Rückfragen, Linearität (1 zu 8 MB).

**Nachweis.** Build Kern-Filter und Windows-Schale 0 Fehler; volles Test-Gate grün (EPOS.Kern.Tests 6 851 und 1 benannt übersprungen, EPOS.UI.Tests 6 243, SpeicherEngine 386, SpeicherPlanung 27 und 1 fremd übersprungen, KiKern 549); SqlDialektPruefer 1 921 SQL-Texte, 0 Fundstellen; Referenzlauf aller dreizehn Projekte gegen R14 13/13 PASS; die geänderten Dateien der iOS-Schale sind in einem Mini-Projekt gegen den Kern übersetzt (die iOS-Workload fehlt unter Windows) — den iOS-Bau selbst zeigt erst der Lauf.

**Offen.** Der eine `ios.yml`-Lauf mit `abnahme_g4a` (nach der Freigabe des Anwenders; Kosten rund 90–125
Läuferminuten, zehnfach angerechnet), danach die Auswertung: iOS-Größengrenzen aus den gemessenen Faktoren,
Trimming-Befund, Größe der xBIM-Anteile im Paket, ggf. `TrimmerRootDescriptor`.

**CI zu G4-8.** `kern.yml` 36106530383 grün.

## 10 Der iOS-Lauf zur Abnahme von G4a (25.09.2026)

`ios.yml` mit Eingang `abnahme_g4a`, Lauf 36106562267 auf dem Stand der Vorbereitung, vom Anwender freigegeben
(E38: genau ein Lauf). **Beide Jobs und alle Schritte grün**; der Gerätebau ist inhaltlich aber nicht bis zum
Ende gekommen (unten).

| Prüfung | Ergebnis |
|---|---|
| Basisnachweis (Simulator, Debug) | Prüfmodus und Startmarken grün; Projekt 1030 gegen R14 **PASS** (236 680 Werte) |
| Getrimmter Bau (Simulator arm64, Release, AOT-only, `TrimMode=partial`, ohne LLVM) | gebaut; **0 IL-Warnungen**; Paket Debug 230 MB, Release 457 MB (AOT-Code, ohne LLVM, nicht die Auslieferungsgröße); die fünf xBIM-DLLs im Release-Paket zusammen 7,4 MB |
| Prüfmodus der getrimmten App | läuft durch; Projekt 1030 gegen R14 **PASS** (Information) |
| xBIM unter Trimming | Metadaten vollständig: IFC2X3 771, IFC4 933, IFC4X3 1 008 Typen — **gleich dem ungetrimmten Stand**; beim Laden keine nicht angelegte Entität, kein leerer Verweis. **Ein `TrimmerRootDescriptor` ist nicht nötig** (Umsetzungskonzept 3.6) |
| Importprobe | **10 von 10 Fällen OK** in 29 s: gbXML-Haus, IFC4-Haus, `.ifczip`, IFC2X3-Haus; synthetisch gbXML 2 / 8 / 9,8 MB und IFC 2 / 8,3 / 20,6 MB — jeder vollständig gelesen (Zahl der Räume wie erzeugt), keine verlorene Entität |
| Dateiwahl | `.ifc` → `public.data`, `.ifcxml`/`.gbxml`/`.xml` → `public.xml`, `.ifczip` → `public.zip-archive`; der Dialog bietet `public.xml`, `public.data`, `public.zip-archive` an |
| Gerätebau `ios-arm64` (Release, ohne Signatur) | Trimmen gelaufen, **0 IL-Warnungen**; der Bau bricht aber beim **abschließenden nativen Linken** ab: `ld: building for 'iOS-simulator', but linking in object file (…/sqlitepclraw.lib.e_sqlite3.ios/2.1.12/static/device/e_sqlite3.a) built for 'iOS'`. Ursache ist die Einbindung der statischen SQLite-Bibliothek der iOS-Schale, **nicht xBIM**; die Schale ist bisher nie für ein Gerät gebaut worden. Geräte-Paketgröße deshalb nicht gemessen. Wegen `continue-on-error` stand der Schritt auf „success" — benannt, nicht übergangen |

**Gemessener Speicher und die iOS-Grenzen (U11, D15).** Simulator, AOT, getrimmt; „Faktor" = Spitzenzuwachs je
MB Datei:

| Fall | Datei | Lesen | verwalteter Zuwachs | Faktor verwaltet | Prozess-Spitze (`phys_footprint`) | Faktor Prozess |
|---|---|---|---|---|---|---|
| gbXML | 2,0 MB | 0,4 s | 16 MB | 8,0 | 166 MB | 7,8 |
| gbXML | 8,0 MB | 1,3 s | 64 MB | 8,0 | 219 MB | 7,7 |
| gbXML knapp unter der Grenze | 9,8 MB | 1,9 s | 79 MB | 8,0 | 237 MB | 7,5 |
| IFC | 2,0 MB | 0,5 s | 40 MB | 19,6 | 205 MB | 14,5 |
| IFC | 8,3 MB | 2,0 s | 101 MB | 12,2 | 287 MB | 10,8 |
| IFC knapp über der Grenze | 20,6 MB | 6,8 s | 222 MB | 10,8 | 407 MB | 9,4 |

Der Prozess stand vor der Probe bei 140–210 MB. **Beide Grenzen (gbXML 10 MB, IFC 20 MB) sind damit gemessen
und mit Reserve bestätigt:** Selbst die 20,6-MB-IFC-Datei hebt den Prozess nur auf rund 400 MB; die Lesedauer
bleibt im Simulator unter 7 s (auf einem iPad langsamer). Die Grenzen bleiben unverändert; eine Anhebung der
gbXML-Grenze auf 25 MB wie unter Windows wäre nach diesen Zahlen vertretbar (rund 200 MB Zuwachs), ist aber
nicht Gegenstand dieses Laufs.

**Abnahmestand G4a:** Importprobe (eigene Proben) bestanden; Referenzlauf unverändert; der iOS-Nachweis unter
Trimming und AOT ist erbracht; offen sind die Windows-Sichtabnahme durch den Anwender und — außerhalb von
G4a — der Gerätelink der iOS-Schale (SQLite).

## 11 Der Gerätelink der iOS-Schale und die Paketgröße mit und ohne xBIM (25.09.2026)

**Auftrag** (Anwender, 25.09.2026): die Ursache des Linkfehlers im Gerätebau aus Lauf 36106562267 finden,
die Einbindung so ändern, dass `ios-arm64` Release ohne Signatur vollständig baut und der Simulatorbau
(Debug und Release) unverändert bleibt; danach die Paketgröße des Gerätebaus mit und ohne xBIM in
ADR-003 (Aufgabe 7) eintragen. Der Nachweis braucht einen iOS-Lauf — nur nach Rückfrage.

**Ursache: Der Gerätebau war ein Simulatorbau.** `EPOS.iOS.csproj` führte
`<RuntimeIdentifiers>iossimulator-arm64;ios-arm64</RuntimeIdentifiers>`. Das iOS-SDK (macios, Zweig
`xcode26.5`, Paket 26.5.10315) entscheidet in `Xamarin.Shared.Sdk.props`:
`_SdkIsSimulator = true`, sobald `RuntimeIdentifier` **oder** `RuntimeIdentifiers` „simulator"
enthält. Diese Datei wird über `WorkloadManifest.targets` erst **nach** der Projektdatei ausgewertet und
sieht deshalb die Liste; verworfen wird die Liste zugunsten des `-r` erst später in
`Xamarin.Shared.Sdk.DefaultItems.targets` (daher die Warnung „RuntimeIdentifier was set on the command
line …"). Mit `-r ios-arm64` stand damit ein gemischter Zustand: Laufzeitpakete des Geräts, aber
`SdkIsSimulator=true` und `ComputedPlatform=iPhoneSimulator` — Link gegen das Simulator-SDK, **kein AOT**
(`_RunAotCompiler` gilt nur für Gerät und `iossimulator-arm64`; im Protokoll des Laufs fehlt jede
AOT-Übersetzung, der Bau dauerte 4 Minuten, als AOT-Objekt liegt allein `main.arm64.o`), `libmono` und
`libxamarin` als dylib. `SQLitePCLRaw.lib.e_sqlite3.ios` 2.1.12 wählt seine Bibliothek in
`build/…ios.targets` über zwei ODER-Bedingungen: `static/device/e_sqlite3.a` bei
`RuntimeIdentifier == ios-arm64` **oder** `ComputedPlatform == iPhone`, `static/simulator/e_sqlite3.a`
bei einer Simulatorkennung **oder** `ComputedPlatform == iPhoneSimulator` — im gemischten Zustand beide.
`ld` baute für „iOS-simulator" und brach an der Gerätebibliothek ab. xBIM und das Paket selbst sind
unschuldig; die Liste war ohnehin wirkungslos (jeder Bau übergibt `-r`, und ohne `-r` wäre die gemischte
Liste ein Fehler des SDK — Gerät und Simulator in einem Bau).

**Änderung.**

| Datei | Änderung |
|---|---|
| `EPOS.iOS/EPOS.iOS.csproj` | `RuntimeIdentifiers` entfernt, Kommentar mit der Begründung; ohne `-r` wählt das SDK den Simulator der Rechnerarchitektur. Schalter `OhneXbim`: `OHNE_XBIM` und `Pruefung/Importprobe.cs`, `Pruefung/Prozessspeicher.cs` aus dem Bau |
| `EPOS.Kern/EPOS.Kern.csproj` | Schalter `OhneXbim` (nur für den Größenvergleich): Paketzeile `Xbim.IO.MemoryModel` bedingt, ohne sie `Allgemein/Import/Ifc/*.cs` außer `IfcImportProfil.cs` und `Importmessung(Proben).cs` aus dem Bau, `OHNE_XBIM` |
| `EPOS.Kern/Allgemein/Import/Ifc/IfcImportProfil.cs` | unter `OHNE_XBIM` lehnt `LeserErzeugen` benannt ab (`PlatformNotSupportedException`; `GebaeudeImportAblauf` legt sie als Lesefehler ab); sonst unverändert |
| `EPOS.iOS/Pruefung/Prueflauf.cs` | Aufruf der Importprobe unter `#if !OHNE_XBIM` |
| `.github/workflows/ios.yml` | Job `geraetebau`: Zielplattform des Programms (`xcrun vtool -show-build`, verlangt `platform IOS`), danach Paket beiseitelegen, `bin`/`obj` räumen, zweiter Bau mit `-p:OhneXbim=true`, Größenvergleich (Summe der Dateien, Programm, xBIM-Dateien, gezipptes Paket, Seed-Datenbank) als Tabelle in Zusammenfassung und Artefakt; Zeitgrenze 75 → 120 Minuten |
| `EPOS.iOS/CLAUDE.md` | Regel „kein `RuntimeIdentifiers`", Absatz zum Gerätebau |

**Simulatorbau.** Mit `-r iossimulator-arm64` war und bleibt `_SdkIsSimulator` gesetzt, die
SQLite-Bedingung wählt allein die Simulatorbibliothek; der Unterschied ist nur, dass der Restore die
Laufzeitpakete des Geräts nicht mehr mitlädt und die RID-Warnung entfällt.

**Lokaler Nachweis** (die iOS-Workload fehlt unter Windows): `EPOS.UI.Daten` samt Kern mit
`-p:OhneXbim=true` 0 Fehler, die Kern-DLL ohne jeden xBIM-Verweis; Kern-Filter im gewöhnlichen Bau
0 Fehler; volles Test-Gate grün (EPOS.Kern.Tests 6 881 und 1 benannt übersprungen, EPOS.UI.Tests 6 243,
SpeicherEngine 386, SpeicherPlanung 27 und 1 fremd übersprungen, KiKern 549). Kein Rechenweg berührt,
deshalb kein Referenzlauf. Den iOS-Bau selbst zeigt erst der Lauf.

**Offen.** Der iOS-Lauf (`ios.yml` mit `abnahme_g4a`, nur nach Rückfrage), danach die Größen in ADR-003
Aufgabe 7.
